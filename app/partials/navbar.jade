nav.navbar.navbar-default.navbar-fixed-top(
  ng-controller='ActivityCtrl'
  role='navigation'
)
  .navbar-inner.normal
    .container
      .navbar-header
        button.navbar-toggle.collapsed(
          ng-click='collapsed = !collapsed'
          type='button'
        )
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar

        a.navbar-brand(href='#/')
          include ../assets/img/logo.svg

      .collapse.navbar-collapse(collapse='collapsed')
        ul#nav-links.nav.navbar-nav(role='menu')
          li: a.upper(href='#')
            fa(name='tachometer')
            | Panel de control
          
          li.active: a.upper(href='#')
            fa(name='flag-checkered')
            | Acciones
          
          li: a.upper(href='#')
            fa(name='folder-o')
            | Archivos
        //-
          li.divider-vertical
          
          li
            a(href='#') Reprogramar

          li
            a(href='#') Enviar informe

        //- form.navbar-form.navbar-right(role='search')
          .form-group
            .input-group
              input.form-control(placeholder='Buscar', type='text')
              .input-group-btn
                button.btn.btn-info(type='button')
                  fa(name='search')
        
        ul#nav-settings.nav.navbar-nav.navbar-right
          li: a(href='#')
            fa(name='gear', size='2')

      .header.toggle.hidden
        include $header
